// CambodiaAddressUI
//
// Presentation layer: the @Observable AddressPickerViewModel, SwiftUI components
// (CambodiaAddressPicker, AddressPickerView), the UIKit wrapper, Environment plumbing,
// and the Localizable.xcstrings UI catalog. The only target that links SwiftUI/UIKit.
//
// Implemented in Phase 4 (see BUILD_PLAN.md).

/// Namespace marker for the UI module. Real types land in Phase 4.
enum CambodiaAddressUI {}
