// CambodiaAddressData
//
// Data layer: datasources (bundled JSON, in-memory, remote stub), the AddressStore
// cache actor, and the DefaultAddressRepository. Hides the on-disk shape behind the
// AddressDataSource boundary.
//
// Implemented in Phase 2 (see BUILD_PLAN.md).

/// Namespace marker for the Data module. Real types land in Phase 2.
enum CambodiaAddressData {}
