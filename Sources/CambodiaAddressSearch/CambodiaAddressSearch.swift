// CambodiaAddressSearch
//
// Offline search engine: Khmer normalizer, prefix trie, inverted token index, and
// bounded fuzzy matching over ~20k villages. Depends only on Core.
//
// Implemented in Phase 3 (see BUILD_PLAN.md).

/// Namespace marker for the Search module. Real types land in Phase 3.
enum CambodiaAddressSearch {}
