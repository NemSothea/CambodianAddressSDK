import Testing
@testable import CambodiaAddressUI

// Placeholder so the test target builds in Phase 0. Real UI/view-model tests land in Phase 4.
@Test func uiModulePlaceholder() {
    #expect(Bool(true))
}
