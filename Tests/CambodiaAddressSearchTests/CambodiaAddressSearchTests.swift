import Testing
@testable import CambodiaAddressSearch

// Placeholder so the test target builds in Phase 0. Real Search tests land in Phase 3.
@Test func searchModulePlaceholder() {
    #expect(Bool(true))
}
