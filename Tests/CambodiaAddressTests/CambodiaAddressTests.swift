import Testing
@testable import CambodiaAddress

// Placeholder so the test target builds in Phase 0. Real facade/integration tests land in Phase 5.
@Test func facadeModulePlaceholder() {
    #expect(Bool(true))
}
