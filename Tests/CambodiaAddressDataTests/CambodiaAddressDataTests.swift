import Testing
@testable import CambodiaAddressData

// Placeholder so the test target builds in Phase 0. Real Data tests land in Phase 2.
@Test func dataModulePlaceholder() {
    #expect(Bool(true))
}
