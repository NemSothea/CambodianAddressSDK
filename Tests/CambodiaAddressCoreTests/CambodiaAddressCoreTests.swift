import Testing
@testable import CambodiaAddressCore

// Placeholder so the test target builds in Phase 0. Real Core tests land in Phase 1.
@Test func coreModulePlaceholder() {
    #expect(Bool(true))
}
